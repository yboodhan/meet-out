import * as mongoose from 'mongoose'
import * as bcrypt from 'bcryptjs'


//create interface extending mongoose.Document (mongoose.Document includes ._id)
// export default interface Event extends mongoose.Document {
  //property keys and data types
  //include types for user and activity object
// }

//create activity schema
// let activitySchema: mongoose.Schema = new moongoose.Schema({
 //event property keys and data types
  //include reference to user and embed activity schema
// })

//create event schema
// let eventSchema: mongoose.Schema = new mongoose.Schema({
//    //property keys and data types
// })


// Create model with type event
// let Event: mongoose.Model<Event> = mongoose.model<Event>('Event', eventSchema)

// module.exports = Event